<Page x:Class="OneDo.Views.Pages.SplashScreenPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Page.Resources>
        <Storyboard x:Name="SplashDismissedStoryboard">
            <DoubleAnimation Storyboard.TargetName="SplashProgressRing"
                             Storyboard.TargetProperty="Opacity"
                             Duration="0:0:1"
                             To="1">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Page.Resources>

    <Grid>
        <Canvas Background="{ThemeResource SystemControlHighlightAccentBrush}">
            <Image x:Name="SplashImage" Source="ms-appx:///Assets/SplashScreen.png"/>
            <ProgressRing x:Name="SplashProgressRing"
                          Foreground="{ThemeResource SystemControlForegroundChromeWhiteBrush}"
                          Opacity="0"
                          IsActive="True"
                          Width="20"
                          HorizontalAlignment="Center" />
        </Canvas>

        <TextBlock x:Name="SplashText"
                   Foreground="{ThemeResource SystemControlForegroundChromeWhiteBrush}"
                   Style="{ThemeResource BodyTextBlockStyle}"
                   Opacity="{Binding Opacity, ElementName=SplashProgressRing}"
                   Text="Loading..."
                   VerticalAlignment="Bottom" 
                   HorizontalAlignment="Center"
                   Margin="24"/>
    </Grid>
</Page>
