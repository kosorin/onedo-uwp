<v:ExtendedPage x:Class="OneDo.View.MainPage"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:behaviors="using:OneDo.View.Behaviors"
                xmlns:controls="using:OneDo.View.Controls"
                xmlns:converters="using:OneDo.View.Converters"
                xmlns:entities="using:OneDo.Model.Data.Entities"
                xmlns:i="using:Microsoft.Xaml.Interactivity"
                xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
                xmlns:v="using:OneDo.View"
                xmlns:vModals="using:OneDo.View.Modals"
                xmlns:vmItems="using:OneDo.ViewModel.Items"
                xmlns:vmModals="using:OneDo.ViewModel.Modals"
                DataContext="{Binding MainPage, Source={StaticResource Locator}}"
                mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries />

            <Style x:Key="NoteItemContainerStyle" TargetType="ListViewItem">
                <Setter Property="Padding" Value="0" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
            </Style>

            <DataTemplate x:Key="NoteItemTemplate" x:DataType="vmItems:NoteItemObject">
                <controls:SlidableListItem Background="{ThemeResource SystemControlBackgroundAltHighBrush}"
                                           LeftBackground="Khaki"
                                           LeftForeground="Black"
                                           LeftGlyph="&#xE7C1;"
                                           LeftLabel="Flag"
                                           MouseSlidingEnabled="True"
                                           RightBackground="Tomato"
                                           RightForeground="White"
                                           RightGlyph="&#xE74D;"
                                           RightLabel="Delete">
                    <Grid Margin="0,8" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <FontIcon Margin="12,0,4,0"
                                      FontSize="18"
                                      Glyph="&#xE8FB;"
                                      Visibility="{x:Bind IsCompleted, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                            <TextBlock Margin="8,0,0,0" Text="{x:Bind Title, Mode=OneWay}" />
                        </StackPanel>
                    </Grid>
                </controls:SlidableListItem>
            </DataTemplate>
        </ResourceDictionary>

    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Folders  -->
        <controls:FolderListControl Grid.Row="0" DataContext="{x:Bind VM.FolderList, Mode=OneWay}" />

        <!--  Notes  -->
        <ListView x:Name="NoteListView"
                  Grid.Row="1"
                  VerticalAlignment="Top"
                  ItemContainerStyle="{StaticResource NoteItemContainerStyle}"
                  ItemTemplate="{StaticResource NoteItemTemplate}"
                  ItemsSource="{x:Bind VM.NoteItems, Mode=OneWay}"
                  SelectedItem="{x:Bind VM.SelectedNoteItem, Mode=TwoWay, Converter={StaticResource ChangeTypeConverter}}">
            <i:Interaction.Behaviors>
                <ic:EventTriggerBehavior EventName="Tapped">
                    <ic:InvokeCommandAction Command="{x:Bind VM.NoteItemTappedCommand}" />
                </ic:EventTriggerBehavior>
            </i:Interaction.Behaviors>
        </ListView>

        <!--  AppBar  -->
        <CommandBar Grid.Row="2">
            <AppBarButton Command="{x:Bind VM.AddNoteCommand}" Label="Add note">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE710;" />
                </AppBarButton.Icon>
            </AppBarButton>
            <CommandBar.SecondaryCommands>
                <AppBarButton Command="{x:Bind VM.ResetDataCommand}" Label="Reset data" />
                <AppBarButton Command="{x:Bind VM.ShowSettingsCommand}" Label="Settings" />
            </CommandBar.SecondaryCommands>
        </CommandBar>

        <!--  Modals  -->
        <controls:ModalContainer Grid.RowSpan="3" />

        <!--  Progress  -->
        <Grid Grid.RowSpan="3">
            <ProgressBar VerticalAlignment="Top"
                         Background="Transparent"
                         DataContext="{Binding ProgressService, Source={StaticResource Locator}}"
                         IsIndeterminate="{Binding IsBusy}" />
        </Grid>

    </Grid>
</v:ExtendedPage>